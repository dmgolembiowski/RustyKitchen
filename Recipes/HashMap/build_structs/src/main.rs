/*
    What is required to loop over all key-value pairs in a HashMap, such that...
    the programmer needs to build new structs and return a  
*/
use std::collections::HashMap;

struct {}

fn main() {
    println!("Hello, world!");
}
